Place the app icon at:

`web/public/brand/app-icon.svg`

The app uses that file for both the sidebar brand mark and browser tab icon. Replace the placeholder SVG in this directory with the final icon, keeping the same filename.
